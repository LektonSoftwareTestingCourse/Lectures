# Lecture 2: Особенности интеграционного тестирования: тестовые данные, изоляция, отладка

> **Модуль**: 6 — Интеграционное тестирование с БД в контейнерах
> **Лектор**: Ирина Габанова
> **Длительность**: 1,5 астрономических часа (90 мин)
> **Слайдов**: 15
> **Формат**: Markdown, каждый слайд — раздел `###`
> **Структура слайда**: Заголовок + 3–5 буллетов + speaker notes
> **Дата**: 03.08.2026
> **Литература**: см. [Источники](#источники) в конце файла

---

## Тайминг

| Время | Блок | Слайды | Назначение |
|:---:|---|:---:|---|
| 0:00–0:15 | Вводный блок | 1–2 | Связь с лекцией 1, план |
| 0:15–0:40 | Тестовые данные | 3–5 | Управление состоянием, транзакции |
| 0:40–1:05 | Изоляция и паттерны | 6–8 | xUnit patterns, Async (Awaitility) |
| 1:05–1:20 | Отладка и CI/CD | 9–12 | Оптимизация, отчёты |
| 1:20–1:30 | Анонс практики | 13–15 | Практика по Logger и Authorization |

---

### Слайд 1. Титульный

**Слайд:**
- Модуль 6. Лекция 2
- Особенности интеграционного тестирования: данные, изоляция, отладка
- Лектор: Ирина Габанова, компания Lekton
- ИТМО, 4 курс

**Speaker notes:**

> Продолжаем Модуль 6. Меня зовут Ирина Габанова, я являюсь Старшим инженером по тестированию в компании Lekton. На прошлой лекции мы научились поднимать базы данных с помощью Testcontainers. Но сама по себе база — это только половина дела. Главная проблема интеграционных тестов — это чистота окружения. Как управлять данными и гарантировать, что тесты не будут падать случайно (flaky)? Об этом мы поговорим сегодня.

---

### Слайд 2. План лекции

**Слайд:**
- Проблема общих фикстур (Shared Fixtures)
- Управление тестовыми данными: Setup и Teardown
- Транзакционность в тестах (Rollback vs Commit)
- Тестирование асинхронных операций (RabbitMQ + Awaitility)
- Паттерны изоляции: Fresh Setup
- Оптимизация и отладка интеграционных тестов в CI

**Speaker notes:**

> Мы разберём анатомию управления состоянием (State Management) [Meszaros, 2007]. Вы узнаете, почему плохо использовать общие данные в тестах и как правильно очищать таблицы PostgreSQL после каждого прогона. Мы также решим проблему тестирования асинхронных очередей в СМП. В конце я дам советы по оптимизации, чтобы ваши тесты в GitHub Actions не висели по 10 минут.

---

### Слайд 3. Проблема общих фикстур

**Слайд:**
- Подход: один раз наполнить БД данными (через init.sql) перед всеми тестами
- Плюс: быстро (нет постоянной очистки)
- Минусы:
  - Тесты зависят друг от друга (порядок запуска важен)
  - Удаление одного теста ломает остальные
  - Сложно отлаживать (состояние постоянно меняется)
- В enterprise это называют "Снежный ком" (Snowball effect)

**Speaker notes:**

> Частая ошибка начинающих — создать в БД 100 карт один раз перед тестами, и потом использовать их во всех методах. Это называется Общая Фикстура (Shared Fixture) [Meszaros, 2007].
> 
> Сначала кажется, что это быстро и удобно. Но что произойдёт? Тест А списал с карты 100 рублей. Тест Б ожидает, что на карте ровно 1000. Тест Б упадёт! И чтобы понять почему, вам придётся гадать, какой тест перед ним запорол состояние. Это прямой путь к хрупким, нестабильным (flaky) тестам, которые все ненавидят.

---

### Слайд 4. Паттерн Fresh Setup

**Слайд:**
- Идеальный мир: каждый тест создаёт свои данные и сам их удаляет
- Подход 1: Очистка таблиц в `@AfterEach`
- Подход 2: Создание данных в `@BeforeEach` с откатом транзакции после теста
- Подход 3: Пересоздание схемы БД перед каждым классом (медленно)
- В Spring Boot: `@DirtiesContext` или `@Transactional`

**Speaker notes:**

> Джерард Месzaros в книге "xUnit Test Patterns" призывает использовать паттерн Fresh Setup [Meszaros, 2007]. Каждый тест должен прийти в чистую "песочницу".
> 
> В Java экосистеме есть лайфхаки. Например, Spring Boot позволяет пометить тест как `@Transactional`. В таком случае все ваши INSERT/UPDATE, сделанные внутри теста, будут автоматически откачены (Rollback) после завершения метода. База останется девственно чистой для следующего теста. Это невероятно удобно и быстро.

---

### Слайд 5. Транзакционность: Проблема Rollback

**Слайд:**
- Если вы тестируете логику, обёрнутую в `@Transactional` в сервисе...
- Тест с `@Transactional` не даст увидеть изменения, пока транзакция не закоммичена
- Внешний поток (Consumer RabbitMQ) не увидит ваши данные
- Решение: использовать `TestTransaction.flagForCommit()` или очистку таблиц вручную

**Speaker notes:**

> Но у Spring-хака с `@Transactional` в тестах есть подвох [Khorikov, 2020]. 
> Допустим, вы тестируете `CardManagementService.save()`, внутри которого есть логика транзакций. Если ваш тест тоже помечен `@Transactional`, Spring объединит эти транзакции. Ни один COMMIT так и не произойдёт.
> Если в этот момент асинхронный Consumer из RabbitMQ попытается прочитать эти данные из БД, он их НЕ УВИДИТ, потому что транзакция не закоммичена. Тест упадёт. В таких случаях нужно принудительно коммитить, либо очищать данные вручную с помощью `@Sql` или JPA `EntityManager.flush/clear`.

---

### Слайд 6. Тестирование RabbitMQ

**Слайд:**
- СМП логирует транзакции асинхронно
- Вызов `api.send()` публикует сообщение и возвращает ответ мгновенно
- База данных Logger-а обновляется через 50-100 мс
- `Thread.sleep(500)` — УЖАСНЫЙ АНТИПАТТЕРН (нестабилен в CI)

**Speaker notes:**

> Переходим к асинхронности. В СМП, когда Switch отправляет ответ "APPROVED", он параллельно кидает сообщение в очередь RabbitMQ для Logger-а. База обновится не сразу.
> 
> Если вы в автотесте вызовете сервис, а потом сразу пойдёте в БД проверять лог, данных там ещё не будет. Тест упадёт.
> 
> Самая частая, но фатальная ошибка — написать `Thread.sleep(2000)`. Да, на вашей машине заработает. В CI под нагрузкой это будет падать в 50% случаев. Никогда не используйте `sleep` для ожидания бизнес-логики.

---

### Слайд 7. Awaitility для асинхронных тестов

**Слайд:**
- Библиотека Awaitility — стандарт для polling (опроса состояния)
- Позволяет писать читаемые условия: "Жди, пока лог не появится в БД, но не более 5 секунд"
- Синтаксис: `await().atMost(5, SECONDS).until(() -> repository.count() == 1)`
- Опрашивает БД каждые 100 мс. Как только условие истинно — тест идёт дальше.
- Идеально для Eventual Consistency

**Speaker notes:**

> Чтобы писать надёжные асинхронные тесты, мы будем использовать библиотеку Awaitility. Это DSL (Domain Specific Language) для опроса состояния.
> 
> Вы пишете: "Жди максимум 5 секунд, пока количество записей в таблице логов не станет равным 1". Awaitility будет опрашивать базу каждые 100 миллисекунд. Как только лог появится — тест мгновенно пойдёт дальше. Если за 5 секунд лог не появился — тест честно упадёт с ошибкой таймаута. Это защищает нас от flaky-тестов [Soto Bueno et al., 2017].

---

### Слайд 8. Изоляция интеграционных тестов

**Слайд:**
- Тесты должны быть независимы (один из принципов FIRST)
- В интеграционных тестах изоляция сложнее из-за БД и брокера
- Очистка очередей RabbitMQ перед/после теста
- Использование уникальных Queue имен для параллельного запуска
- Имена очередей с префиксом: `test-transaction-queue-{UUID}`

**Speaker notes:**

> Чтобы не наступать на грабли конкуренции, в асинхронных тестах иногда удобно динамически создавать временные очереди для каждого тест-класса. 
> Например, вместо того чтобы чистить боевые очереди СМП перед каждым прогоном, вы указываете Testcontainers создать новую очередь с суффиксом UUID. Это гарантирует, что два теста, запущенные параллельно, не будут читать сообщения друг друга [Soto Bueno et al., 2017].

---

### Слайд 9. Отладка интеграционных тестов

**Слайд:**
- "Мой тест падает в CI, но работает локально". Знакомо?
- Использование `@ServiceConnection` (Spring Boot 3.1+) для лёгкой настройки
- Включение SQL логов: `spring.jpa.show-sql=true`
- Снятие дампа БД в конце теста
- Отключение Testcontainers cleanup для ручного дебага

**Speaker notes:**

> Отладка интеграционных тестов может стать адом. 
> Первое правило: включите логирование SQL-запросов, чтобы видеть, какие запросы летят в БД.
> Второе: если тест работает локально, но падает в CI — скорее всего, проблема во времени (race condition) или в различиях окружения (кодировка, часовой пояс). Awaitility решает проблему времени.
> Третье: если вы не понимаете, почему тест упал, запретите Testcontainers удалять контейнер, подключитесь к нему в конце теста через DBeaver и посмотрите данные.

---

### Слайд 10. Оптимизация скорости

**Слайд:**
- Интеграционные тесты медленные (секунды, а не миллисекунды)
- Паттерн Singleton Container: БД поднимается один раз для всех тестов
- Переиспользование контейнеров между классами с помощью `@Testcontainers`
- Параллельный запуск тестов (JUnit 5 `@Execution(CONCURRENT)`)
- Баланс: Unit-тесты должны покрывать логику, Интеграционные — только интеграцию

**Speaker notes:**

> Если вы напишете 50 интеграционных тестов, и каждый будет поднимать свой PostgreSQL контейнер, ваш прогон будет длиться вечность.
> 
> Решение — паттерн Singleton Container. Вы объявляете контейнер как `static` поле в базовом классе. Контейнер стартует один раз перед всеми тестами в suite, а тесты между собой очищают таблицы (через `@Transactional` или `TRUNCATE`). Это снижает время прогона с минут до нескольких секунд [Soto Bueno et al., 2017].

---

### Слайд 11. Quality Gates для интеграции

**Слайд:**
- В CI интеграционные тесты часто выделяют в отдельную Job (Stage)
- Maven Failsafe плагин: классы должны называться `*IT.java`
- Сборка падает, если хотя бы один IT-тест красный
- Allure Reports собирают результаты и скриншоты БД

**Speaker notes:**

> В мире Maven принято разделять Unit и Integration тесты. Unit-тесты запускаются плагином Surefire (классы `*Test.java`), а интеграционные — плагином Failsafe (классы `*IT.java`).
> 
> Это позволяет вам запускать быстрые Unit-тесты на каждое нажатие кнопки в IDE, а тяжёлые интеграционные — только перед коммитом или в CI Pipeline.

---

### Слайд 12. Архитектурный паттерн: "Тестируемый дизайн"

**Слайд:**
- Код с глобальным состоянием (static переменные) невозможно тестировать изолированно
- Внедрение зависимостей (Dependency Injection) упрощает моки и подмены
- Внешние вызовы (HTTP, БД) должны быть обёрнуты в интерфейсы
- `CardManagementClient` -> `interface CardManagementClient` -> `Impl` и `TestImpl`

**Speaker notes:**

> Стивен Фримен и Нэт Прайс учат нас [Freeman & Pryce, 2009]: код, который сложно тестировать, — это код с плохой архитектурой. 
> Если ваш сервис жёстко привязан к конкретному URL RabbitMQ или использует статические методы для записи в БД, вы не сможете его изолировать. Выделение интерфейсов и использование Spring Dependency Injection — залог того, что вы легко сможете вставить Testcontainers в тестах.

---

### Слайд 13. Что будет на практике

**Слайд:**
- Практика 12: Интеграционные тесты для Transaction Logger и Authorization
- Совместная отладка (почему тест падает в CI)
- Настройка Awaitility для проверки логирования
- Использование Singleton паттерна для PostgreSQL

**Speaker notes:**

> На практике 12 мы продолжим писать интеграционные тесты для Transaction Logger и Authorization Service. Главный акцент будет на отладке асинхронных сценариев. Мы подключим Awaitility, чтобы тесты корректно дожидались записи в лог, и настроим чистку таблиц между прогонами. 

---

### Слайд 14. Итоги Модуля 6

**Слайд:**
- Интеграционные тесты гарантируют реальное взаимодействие с БД и брокерами
- Управление состоянием — ключ к стабильным тестам (Fresh Setup)
- Awaitility решает проблему Eventual Consistency
- Singleton Containers экономят время в CI
- Интеграционные тесты лежат в основе артефакта 4

**Speaker notes:**

> Завершаем Модуль 6. Теперь ваш арсенал тестировщика пополнился мощнейшим инструментом — Testcontainers и техниками работы с асинхронностью. В следующем модуле мы объединим всё изученное и перейдём к сквозному E2E тестированию.

---

### Слайд 15. Вопросы и ответы

**Слайд:**
- Подготовка к Модулю 7 (E2E)
- Разбор сложных моментов с Testcontainers
- Вопросы?

**Speaker notes:**

> Вопросы по практике? Если у кого-то Testcontainers не скачивает образы, проверьте настройки Docker Desktop и выделенную память. Минимум 4 ГБ ОЗУ для СМП обязателен.

---

## Источники

1. **Soto Bueno, A., Porter, J., & Gumbrecht, A.** (2017). *Testing Java Microservices: Using Arquillian, Hoverfly, AssertJ, JUnit, Selenium, and Mockito*. Manning.
2. **Meszaros, G.** (2007). *xUnit Test Patterns: Refactoring Test Code*. Addison-Wesley.
3. **Khorikov, V.** (2020). *Unit Testing: Principles, Practices, and Patterns*. Manning.
4. **Freeman, S., & Pryce, N.** (2009). *Growing Object-Oriented Software, Guided by Tests*. Addison-Wesley.
5. **Humble, J., & Farley, D.** (2010). *Continuous Delivery: Reliable Software Releases through Build, Test, and Deployment Automation*. Addison-Wesley.