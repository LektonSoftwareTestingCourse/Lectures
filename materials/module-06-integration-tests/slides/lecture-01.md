# Lecture 1: Интеграционное тестирование: БД, очереди, внешние сервисы

> **Модуль**: 6 — Интеграционное тестирование с БД в контейнерах
> **Лектор**: Ирина Габанова
> **Длительность**: 1,5 астрономических часа (90 мин)
> **Слайдов**: 15
> **Формат**: Markdown, каждый слайд — раздел `###`
> **Структура слайда**: Заголовок + 3–5 буллетов + speaker notes
> **Дата**: 03.08.2026
> **Литература**: см. [Источники](#источники) в конце файла

---

## Тайминг

| Время | Блок | Слайды | Назначение |
|:---:|---|:---:|---|
| 0:00–0:15 | Вводный блок | 1–3 | Знакомство, план, что такое интеграционное тестирование |
| 0:15–0:40 | Интеграции и БД | 4–6 | Стратегии, БД в тестах, миграции |
| 0:40–1:05 | Очереди и Внешние сервисы | 7–9 | RabbitMQ, WireMock |
| 1:05–1:20 | Testcontainers | 10–13 | Архитектура, примеры, Spring Boot Test |
| 1:20–1:30 | Итоги и практика | 14–15 | Анонс артефакта 4 (API и интеграция) |

---

### Слайд 1. Титульный

**Слайд:**
- Курс «Тестирование программного обеспечения»
- Модуль 6. Интеграционное тестирование с БД в контейнерах
- Лектор: Ирина Габанова, компания Lekton
- ИТМО, 4 курс

**Speaker notes:**

> Добрый день. Меня зовут Ирина Габанова, я являюсь Старшим инженером по тестированию в компании Lekton. Мы завершаем знакомство с изолированными тестами и делаем важнейший шаг — начинаем тестировать то, как наши сервисы общаются с внешним миром: базами данных, очередями сообщений и другими микросервисами. Добро пожаловать в Модуль 6, посвящённый интеграционному тестированию с использованием Testcontainers.

---

### Слайд 2. План лекции

**Слайд:**
- Что такое Интеграционное тестирование и где оно живёт в V-модели
- Интеграционные стратегии: Top-Down, Bottom-Up, Sandwich
- Проблема тестирования баз данных: H2 vs Real PostgreSQL
- Миграции БД (Flyway) в тестах
- Тестирование брокера сообщений RabbitMQ
- Знакомство с Testcontainers и Spring Boot Test

**Speaker notes:**

> Сегодня мы разберём, как проверять корректность взаимодействия компонентов. Мы поймём, почему Unit-тестов недостаточно. 
> Мы изучим современные подходы к тестированию баз данных, в чём вред использования in-memory баз типа H2 для SQL-приложений, и почему индустрия перешла на Docker-контейнеры. Также мы коснёмся асинхронного тестирования — как проверять отправку сообщений в RabbitMQ.

---

### Слайд 3. Что такое Интеграционное тестирование

**Слайд:**
- Уровень тестирования, проверяющий взаимодействие между модулями/сервисами
- Проверка контрактов (API), работы с БД, сериализации/десериализации
- В V-модели соответствует фазе Детального дизайна
- Отвечает на вопрос: «Правильно ли компоненты взаимодействуют?»
- В СМП: проверка, что Authorization Service корректно пишет транзакции в PostgreSQL

**Speaker notes:**

> Вспомним V-модель из Модуля 1 [Spillner et al., 2014, Ch.3]. Unit-тесты проверяют код. Интеграционные тесты проверяют связи. 
> Когда вы пишете код репозитория на JPA/Hibernate, вы можете замокать EntityManager в Unit-тесте. Но это не докажет, что ваш SQL-запрос написан правильно, или что миграции Flyway применяются без ошибок. Интеграционные тесты проверяют реальные границы систем. В enterprise-системах Lekton львиная доля багов кроется именно на стыке сервисов, поэтому этот уровень тестирования критически важен.

---

### Слайд 4. Интеграционные стратегии

**Слайд:**
- **Top-Down**: сначала верхние уровни (UI/API), нижние мокируем (Stubs)
- **Bottom-Up**: снизу вверх. Сначала тестируем БД и базовые сервисы
- **Sandwich**: гибридный подход (встреча посередине)
- В микросервисах чаще всего: Bottom-Up внутри сервиса + Contract Tests между сервисами

**Speaker notes:**

> Существует несколько стратегий сборки системы для интеграции [Spillner et al., 2014, 3.3.5]. В классической монолитной разработке можно было двигаться сверху вниз (от UI к БД) или снизу вверх.
> В микросервисной архитектуре, какой у нас в СМП, мы обычно тестируем интеграцию внутри одного сервиса. То есть, берём Authorization Service, реальную БД PostgreSQL, и проверяем, что он правильно стартует, делает SELECT и UPDATE. А вот внешние вызовы (к Card Management) мы пока мокируем или используем Контракты.

---

### Слайд 5. Тестирование БД: Иллюзия H2

**Слайд:**
- Старый подход: использовать H2 (in-memory) базу для тестов
- Проблема: диалекты SQL различаются
- Код работает на PostgreSQL в проде, но падает на H2 в тестах (или наоборот)
- Скрытые баги в миграциях (Flyway/Liquibase)
- Принцип: "Тестируй то, на чём будешь запускать" (Test what you fly, fly what you test)

**Speaker notes:**

> Долгое время стандартом было использовать встраиваемые базы данных вроде H2 для прогона тестов. Они быстрые. Но Владимир Хориков [Khorikov, 2020, Ch.10] и другие эксперты называют это антипаттерном.
> 
> H2 поддерживает свой диалект SQL. PostgreSQL — свой. Вы можете написать код, который отлично проходит тесты на H2, но падает с ошибкой синтаксиса на проде. Или используемый вами тип данных JSONB вообще не поддерживается в H2. Единственный надёжный способ интеграционного тестирования БД — поднимать реальную СУБД.

---

### Слайд 6. Миграции БД (Flyway)

**Слайд:**
- Version Control для вашей базы данных
- Миграции: V1__init.sql, V2__add_card_status.sql
- В тестах БД должна создаваться с нуля (прогон всех миграций)
- Гарантирует, что свежий сервер можно развернуть без ошибок
- В СМП: проверка DDL скриптов в `src/main/resources/db/migration`

**Speaker notes:**

> Миграции БД (инструменты вроде Flyway или Liquibase) — это git для вашей схемы данных. 
> В каждом интеграционном тесте вы должны создавать чистую базу, применять все скрипты миграций от V1 до текущего момента, и только потом запускать тесты. Если разработчик забыл добавить колонку в очередной `V5__add_limits.sql`, ваш интеграционный тест упадёт с `Column not found`. И это прекрасно — мы поймали баг миграции до прода.

---

### Слайд 7. Тестирование RabbitMQ

**Слайд:**
- RabbitMQ — брокер сообщений (асинхронное взаимодействие)
- В Unit-тестах мы замокировали шаблоны (RabbitTemplate)
- В интеграционных нам нужно проверить:
  - Публикацию сообщения в Exchange
  - Маршрутизацию в правильную Queue
  - Десериализацию сообщения Consumer-ом
- Без реального брокера это невозможно проверить полноценно

**Speaker notes:**

> В архитектуре СМП Switch публикует транзакцию в RabbitMQ, а Logger её сохраняет. Как проверить, что сообщение реально долетит и формат JSON совпадёт? Мок тут не поможет [Soto Bueno et al., 2017]. 
> Нам нужен реальный брокер, чтобы убедиться, что сообщение проходит обмен (Exchange), привязки (Bindings) и доходит до очреди Transaction Queue.

---

### Слайд 8. Тестирование внешних API

**Слайд:**
- Authorization Service обращается к внешнему Bin Lookup
- Нельзя в тестах дёргать реальные внешние системы (нестабильность, лимиты, токены)
- Инструменты: WireMock, Hoverfly
- Имитация ответов 200 OK, таймаутов, ошибок 500
- Контрактное тестирование (Consumer-Driven Contracts)

**Speaker notes:**

> Когда сервис зависит от чужого API (допустим, платёжной системы Visa или нашего Bin Lookup), мы не можем писать автотесты, которые реально ходят в интернет. Сеть может упасть, будут списываться деньги, нарушатся лимиты.
> 
> Для этого используются инструменты-прокси, такие как WireMock [Soto Bueno et al., 2017]. Они перехватывают HTTP-запросы и возвращают заранее заготовленные ответы. Так мы можем легко сэмулировать таймаут Bin Lookup и проверить, что наш Authorization Service корректно обработает ошибку (fallback).

---

### Слайд 9. Знакомство с Testcontainers

**Слайд:**
- Библиотека для Java (и не только), предоставляющая легковесные API к Docker
- В `@BeforeAll` (или через аннотации) поднимает реальную БД или RabbitMQ в контейнере
- Выполняет тест, затем уничтожает контейнер
- Изоляция: каждый прогон — чистое окружение
- Решает проблему H2 и Mockito для инфраструктуры

**Speaker notes:**

> И вот мы подошли к самому сладкому. Инструмент, перевернувший мир интеграционного тестирования в Java — Testcontainers [Soto Bueno et al., 2017]. 
> Это библиотека, которая программно обращается к Docker на вашей машине. Вы пишете:
> `PostgreSQLContainer postgres = new PostgreSQLContainer("postgres:16");`
> Testcontainers скачивает образ, запускает контейнер, отдаёт вам JDBC URL. Вы подключаете JPA, прогоняете тест, и контейнер удаляется. Это даёт 100% уверенность, что вы тестируете реальный PostgreSQL 16 версии.

---

### Слайд 10. Архитектура Testcontainers

**Слайд:**
- Связь через Docker Daemon (TCP socket)
- Контейнеры запускаются из образов (Docker Hub или локальные)
- Поддержка Wait Strategies: ожидание готовности (логи, порты)
- Использование `@Testcontainers` и `@Container` в JUnit 5
- Singleton контейнеры для оптимизации времени прогона

**Speaker notes:**

> Как работает Testcontainers под капотом? Он запускает специальный контейнер (Ryuk), который управляет жизненным циклом. Библиотека сама умеет ждать, когда база данных стартует. Согласитесь, запуск PostgreSQL занимает пару секунд, и база не готова принимать коннекты мгновенно. Testcontainers читает логи и ждёт строчки "database system is ready to accept connections".
> 
> Чтобы не поднимать базу перед каждым методом, можно использовать паттерн Singleton-контейнера (поднять контейнер один раз на класс или весь test suite) [Soto Bueno et al., 2017].

---

### Слайд 11. Spring Boot Test Integration

**Слайд:**
- Аннотация `@SpringBootTest` поднимает Application Context
- `@DataJpaTest`: срез для тестирования репозиториев (настраивает БД и Hibernate)
- `@TestPropertySource`: подставляем URL от Testcontainers в properties Spring
- Автоматическое применение Flyway при старте контекста

**Speaker notes:**

> В экосистеме Spring Boot всё работает как по маслу [Tahchiev et al., 2015]. Если нам нужно проверить только слой репозиториев (JPA), мы используем аннотацию `@DataJpaTest`. Spring Boot поднимает только нужные бины.
> 
> Мы перехватываем свойства (application.yml), подменяем там URL базы данных на тот, который сгенерировал Testcontainers, и Spring даже не подозревает, что работает с тестовым контейнером. Он применяет миграции Flyway и поднимает схему.

---

### Слайд 12. Интеграционные тесты в СМП

**Слайд:**
- **Цель (Артефакт 4)**: Написать интеграционные тесты для сервисов СМП
- Поднять PostgreSQL 16 в Testcontainers
- Проверить репозитории Transaction Logger и Card Management (SELECT/UPDATE)
- Проверить публикацию сообщения в RabbitMQ
- Интеграция с CI: контейнер должен стартовать в GitHub Actions

**Speaker notes:**

> Ваш Артефакт 4 будет посвящён именно этому. Вы возьмёте сервис Card Management и напишете интеграционный тест, который поднимет реальный PostgreSQL, применит миграции и проверит, что метод `reserveAmount()` реально уменьшает баланс в БД. 
> Также CI-система в GitHub Actions должна уметь это прогонять (Docker там доступен из коробки).

---

### Слайд 13. Отладка Testcontainers

**Слайд:**
- Тесты падают непонятно? Контейнер удалился太快 (too fast)?
- Включение логов: `withLogConsumer(output -> System.out.print(output.getUtf8String()))`
- Режим отладки: защита от удаления контейнера после теста
- Использование Testcontainers Desktop или прямого доступа к Docker

**Speaker notes:**

> Частый вопрос: "Мой интеграционный тест падает с ошибкой SQL, но контейнер сразу закрывается, я не вижу данных в БД". 
> В Testcontainers есть режим отладки. Вы можете сказать контейнеру не удаляться, зайти в него руками через консоль, посмотреть данные, выполнить `SELECT * FROM cards`, и понять, почему логика отработала не так. Мы научимся этому.

---

### Слайд 14. Что будет на практике

**Слайд:**
- Практика 11: Поднимаем PostgreSQL и RabbitMQ в контейнерах
- Пишем интеграционные тесты для Card Management с Testcontainers
- Настройка `pom.xml`, добавление зависимости
- Запуск теста локально, наблюдение за логами Docker
- 7 баллов в БаРС

**Speaker notes:**

> Переходим к практике 11. Вы добавите библиотеку Testcontainers в ваш проект, мы вместе поднимем PostgreSQL контейнер и напишем первый интеграционный тест, который реально дёрнет базу. Вы увидите, насколько это надёжнее, чем мокировать всё подряд.

---

### Слайд 15. Итоги лекции

**Слайд:**
- Интеграционное тестирование проверяет реальное взаимодействие (БД, Очереди)
- Использование H2 и моков для инфраструктуры — вредно
- Testcontainers позволяет тестировать реальные версии СУБД
- Интеграционные тесты гарантируют работоспособность миграций
- Вопросы?

**Speaker notes:**

> Сегодня мы сделали огромный шаг вперёд. Мы перешли от "игрушечных" тестов с моками к проверке реальной инфраструктуры. В современной enterprise-разработке Testcontainers — это абсолютный мастхэв. Вопросы?

---

## Источники

1. **Soto Bueno, A., Porter, J., & Gumbrecht, A.** (2017). *Testing Java Microservices: Using Arquillian, Hoverfly, AssertJ, JUnit, Selenium, and Mockito*. Manning. ISBN 978-1617294458.
2. **Spillner, A., Linz, T., & Schaefer, H.** (2014). *Software Testing Foundations: A Study Guide for the Certified Tester Exam* (4th ed.). Rocky Nook.
3. **Khorikov, V.** (2020). *Unit Testing: Principles, Practices, and Patterns*. Manning. (Глава 10: Testing the database).
4. **Tahchiev, P., Leme, F., Massol, V., & Gregory, G.** (2015). *JUnit in Action, Second Edition*. Manning.